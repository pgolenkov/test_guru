<div>
  <%= link_to 'К списку тестов', tests_path %>
</div>

<h1><%= test_header(@test) %></h1>

<%= render 'shared/errors', resource: @test %>

<%= form_with model: @test, local: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, id: :test_title %>
  <%= f.label :level %>
  <%= f.number_field :level, id: :test_level %>
  <%= f.label :category_id %>
  <%= f.collection_select :category_id, Category.all, :id, :title, prompt: true, id: :test_category_id %>
  <%= f.submit %>
<% end %>
