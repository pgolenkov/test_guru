<div class="row mt-2 align-items-center text-center">
  <div class="col-2 badge-image">
    <%= image_tag badge.image %>
  </div>
  <div class="col-3"><%= badge.name %></div>
  <div class="col-4"><%= badge.rule.name %></div>
  <div class="col-1"><%= @user_badges.where(badge: badge).count %></div>
</div>
